{
  "name": "local-whisper-extension",
  "version": "1.0.0",
  "description": "Local Whisper AI transcription Chrome extension",
  "type": "module",
  "dependencies": {
    "@xenova/transformers": "^2.15.1"
  },
  "devDependencies": {
    "bestzip": "^2.2.1",
    "esbuild": "^0.20.1",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "build": "node build.js",
    "build:prod": "node build-prod.js",
    "clean": "rimraf dist dist-prod",
    "zip": "cd dist-prod && bestzip ../whisper-transcribe.zip *"
  }
}
